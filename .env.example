# Copy this file to .env.local and fill in. .env.local is gitignored — never commit real values.

# --- CLIENT-SAFE (used in the browser; RLS protects the data) ---
SUPABASE_URL=https://dxhravqtmmxeubdwxblo.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_m2rIXmKjXjV0b91EyOrIfQ_IYGsnoj3

# --- SECRET (server-side ONLY — Vercel env vars / admin scripts) ---
# Get these from Jiten. NEVER commit. NEVER put in the browser or an AI tool.
SUPABASE_SECRET_KEY=
SUPABASE_DB_PASSWORD=
